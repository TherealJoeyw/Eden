services:
  bot:
    build: ${COMPOSE_BUILD_PATH:-/mnt/pool/Apps/Eden}
    env_file:
      - ${COMPOSE_BUILD_PATH:-/mnt/pool/Apps/Eden}/.env
    # Add BACKUP_PATH=/backups to the .env file used above.
    depends_on:
      - postgres
    volumes:
      - /mnt/pool/Apps/Eden/backups:/backups
    restart: unless-stopped

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: eden
      POSTGRES_USER: eden
      POSTGRES_PASSWORD: eden
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
